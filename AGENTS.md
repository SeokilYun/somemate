# 썸메이트 (Somemate)

대화 캡처와 고민을 공유하면 성격이 다른 AI 친구 3명(낙관/신중/현실)이 각자의 관점으로 해석해주는 관계 상담 웹앱. 연애뿐 아니라 친구·동료·가족 관계도 다룬다. 모바일 우선 반응형 웹앱.

## 역할 분담

3인 협업 기준으로 담당을 나눈다. 각자 바이브 코딩으로 맡은 영역을 구현하고, 기능별 최종 책임자를 둔다.

| 담당 | 역할 | 실제 담당 업무 | 책임지는 결과 |
| --- | --- | --- | --- |
| A — 기획·UX 담당 | 서비스 흐름과 사용자 경험 설계 | MVP 범위·이용 정책 확정, 전체 화면 디자인, 로그인 화면·상대방 정보 입력·친구 초대 화면 구현, 사용자 테스트 진행 | 사용자가 서비스를 이해하고 상담을 시작할 수 있는가? |
| B — AI 상담 담당 | 서비스의 핵심 상담 경험 구현 | 채팅 화면·사진 첨부·이미지 분석·캐릭터별 응답 구현, 캐릭터 프롬프트 조정, 이전 맥락을 반영한 후속 대화 | 세 캐릭터의 분석이 구분되고, 실제로 도움이 되는가? |
| C — 서비스 기반·운영 담당 | 서비스가 안정적으로 작동하는 기반 구현 | 실제 회원가입·로그인 처리, 이미지·대화 저장, 사용자별 접근 권한, 푸시 예약·발송, 배포, 오류·비용 확인 | 기록이 유지되고, 알림과 서비스 운영이 정상적으로 이루어지는가? |

A는 화면 구현과 사용자 테스트까지 맡아야 세 역할의 작업량이 균형을 이룬다. B·C가 개발하는 동안 A는 테스트 사용자를 모집하고, 실제 이용에서 막히는 부분을 찾아 다음 수정사항으로 정리한다.

### 기능별 역할 경계

같은 기능이라도 화면/구현/운영 단계에서 담당이 바뀌므로, 경계를 아래처럼 고정한다.

| 기능 | 역할 경계 |
| --- | --- |
| 로그인 | A가 화면과 입력 안내 구현 → C가 실제 인증·로그인 상태 유지 구현 |
| 상대방 정보 | A가 입력·수정 화면 구현 → C가 저장·조회·접근 권한 구현 |
| 캐릭터 | A가 성격·말투·응답 기준 정의 → B가 프롬프트와 응답 생성 구현 |
| 사진 분석 | B가 첨부 화면·이미지 해석 구현 → C가 이미지 저장·삭제·접근 권한 구현 |
| 대화 기록 | B가 채팅 표시·맥락 활용 구현 → C가 저장·조회 구조 제공 |
| 후속 알림 | A가 발송 시점·문구·취소 조건 정의 → C가 예약·발송 구현 → B가 상담방 복귀 후 대화 연결 |

### 협업 원칙

- **공통 기획서 하나를 유지한다.** 화면 흐름, 필드 이름, 캐릭터 성격, 알림 조건, 이번 출시 범위는 이 문서(AGENTS.md)와 [docs/API.md](docs/API.md)에만 적고, 세 사람 모두 AI에게 요청할 때 이 문서를 공통으로 제공한다.
- **데이터와 연결 방식을 먼저 합의한다.** User·Partner·Conversation·Message에 어떤 정보가 들어가는지는 위 [데이터 모델](#데이터-모델-prisma-개요)을 기준으로 맞추고, 임의로 필드를 바꾸지 않는다. 예: 모두 같은 `Conversation.id`를 사용해야 대화 저장과 알림 이동이 연결된다.
- **작업 영역과 변경 범위를 명시한다.** 각자 별도 브랜치에서 작업하고, AI에게 담당 화면·파일과 수정 범위를 알려준다. 공통 디자인이나 데이터 구조 변경은 셋이 공유한 뒤 반영한다.
- **작게 연결하며 검증한다.** 먼저 '로그인 → 상대방 저장 → 상담방 생성'을 연결하고, 다음으로 사진 분석과 기록 저장, 마지막으로 후속 알림을 붙인다. 매 작업일이 끝날 때 함께 실행해 연결 상태를 확인한다.
- **구현자 외에 검수자를 지정한다.** A가 B의 캐릭터 응답을 검수하고, B가 C의 저장·알림 동작을 확인하고, C가 A의 화면과 실제 데이터 연결을 확인한다. 최종 병합·배포는 C가 맡는다.

## 기술 스택

- Next.js (App Router) + TypeScript
- DB: SQLite + Prisma (로컬/단일 인스턴스 배포 기준, 추후 Postgres 전환 용이)
- 인증: 이메일/비밀번호, NextAuth Credentials Provider + bcrypt 해시, JWT 세션
- 이미지 저장: 로컬 `uploads/` (또는 `public/uploads`), 사용자 ID로 경로 격리
- 이미지 분석 & 캐릭터 응답: Anthropic API (vision 지원 모델), 서버 라우트에서만 호출
- 푸시: Web Push (VAPID) + Service Worker, 구독 정보 DB 저장, 서버에서 `web-push`로 발송
- 스타일: Tailwind CSS

## 데이터 모델 (Prisma 개요)

- `User`: id, email, passwordHash, createdAt
- `Partner`(상담 대상 정보): id, userId, name, age?, mbti?, interests(string[] 또는 JSON), relationship, relationshipCustom?
- `Conversation`(상담방): id, userId, partnerId, createdAt, lastMessageAt, firstAnalysisAt?, followupScheduledAt?, followupSentAt?
- `Message`: id, conversationId, role(user/optimistic/cautious/realistic/system), content, imageUrl?, createdAt
- `PushSubscription`: id, userId, endpoint, keys(JSON)

모든 조회는 `userId` 기준으로 스코프 — 본인 소유가 아닌 `Conversation`/`Message`/이미지 파일에는 접근 불가하도록 서버에서 검증.

## 화면/기능 명세

### 1. 인증
- 이메일/비밀번호 회원가입·로그인, 세션 유지, 로그아웃.
- 폼 검증 에러, 로그인 실패, 로딩 상태 UI 처리.

### 2. 메인페이지
- 신규 사용자: "누구에 대한 이야기를 나눠볼까?" + Partner 입력 폼.
  - 필수: 이름, 관계(썸/소개팅/동료/친구/부모님/기타+직접입력)
  - 선택: 나이(숫자), MBTI(16유형+모름), 관심사(태그 입력/삭제)
  - 필수값 충족 시 "친구 초대하기" 버튼 활성화.
- 기존 사용자: 상담 카드 리스트(닉네임, 관계, 마지막 메시지 미리보기, 마지막 대화 시각) → 클릭 시 해당 채팅방 이동.
- Partner 정보는 상담방 생성 후에도 수정 가능.

### 3. 친구 초대
- 낙관/신중/현실 3개 카드: 아바타, 이름, 성격 설명, 대표 한마디.
  - 낙관: "좋은 신호가 있는지 같이 찾아보자!"
  - 신중: "아직 모르는 부분도 있으니까, 천천히 보자."
  - 현실: "말과 행동을 기준으로 정리해줄게."
- "세 친구 초대하고 시작하기" → Conversation 생성 후 채팅방으로 이동. MVP는 3명 전원 참여 고정.

### 4. 채팅방
- 레이아웃: 상단(닉네임/관계/캐릭터), 중앙(메시지 스트림), 하단(사진 첨부/텍스트 입력/전송).
- 신규 방 최초 진입 시 3캐릭터 인사 1회 자동 출력(낙관→신중→현실 순) + 업로드 안내 메시지. 재진입 시 인사 반복 금지, 마지막 메시지부터 이어서 표시.
- 이미지 업로드: 1장, 전송 전 미리보기/취소, 상황 설명 텍스트는 선택. 전송 시 처리중 상태 표시.
- 분석 결과: 낙관→신중→현실 순서로 메시지 렌더링. 각 메시지는 (해석)+(근거로 삼은 표현/행동)+(다음에 해볼 행동)을 포함.
  - 규칙: 낙관은 무조건 긍정 금지, 현실은 공격적 어투 금지. 나이/MBTI는 보조 참고, 실제 대화 내용이 분석 중심. 속마음 단정·근거 없는 호감도 수치 금지. 텍스트를 읽기 어렵거나 화자 불분명 시 추측 금지, 사용자에게 확인 질문.
- 후속 대화: 텍스트/새 이미지로 이어감, 캐릭터는 Partner 정보 + 이전 대화 맥락 반영.

### 5. 후속 알림
- 첫 분석 완료 시 "내일 이 이야기 다시 나눠볼까?" + 알림 신청 버튼 노출.
- 신청 시 알림 권한 요청 → 24시간 뒤 1회 발송: "그 이야기, 어떻게 됐어? 이어서 이야기해볼까?"
- 사용자가 예약 전에 해당 방에서 먼저 후속 대화를 시작하면 예약 취소.
- 사용자가 직접 예약 취소 가능.
- 알림 클릭 → 로그인 필요 시 로그인 후 해당 상담방으로 이동, 진입 시 "지난번 이야기, 그 뒤로 어떻게 됐어?" 안내.
- 알림 미허용이어도 사용자가 직접 방에 들어가 대화 계속 가능.

## 디자인 톤

밝은 배경 + 부드러운 포인트 컬러, 읽기 편한 대비. 3캐릭터는 아바타/이름/포인트 컬러로 구분. 모바일 터치 타겟(버튼, 사진 첨부) 우선 최적화. 빈 화면/로딩/입력 오류/분석 실패·재시도 상태 모두 디자인.

## 보안/데이터 규칙

- Anthropic API 키 등 비밀 정보는 서버(환경 변수)에서만 사용, 클라이언트 노출 금지.
- 모든 API 라우트는 세션의 userId로 소유권 검증 후 데이터 반환.
- 업로드 이미지는 소유자만 접근 가능한 경로/서명으로 서빙.

## 구현 범위 구분

- **실제 동작**: 이메일/비밀번호 인증, 상담방/메시지 영속 저장(새로고침·재로그인 유지), 이미지 업로드+Anthropic vision 분석, 캐릭터 3인 응답, Web Push(화면 닫아도 수신).
- **환경 설정 필요**: `ANTHROPIC_API_KEY`, VAPID 키 쌍(`VAPID_PUBLIC_KEY`/`VAPID_PRIVATE_KEY`), 알림 발송 스케줄 실행 방식(로컬은 cron/서버 프로세스로 24h 지연 작업 처리 — 배포 환경에 맞는 스케줄러 확인 필요).
- **데모 표시 필요**: 위 설정 없이 목업 데이터로 대체하는 화면이 있다면 화면 내 "데모" 배지로 명시하고, 타이머 기반 화면 내 알림을 실제 푸시로 오인시키지 않을 것.

## API 스펙

엔드포인트/요청·응답 형식은 [docs/API.md](docs/API.md) 참고.

## 검증 체크리스트 (구현 후 직접 확인)

회원가입·로그인 → 상대방 정보 저장 → 친구 초대 → 사진 업로드 → 캐릭터 3명 분석 → 추가 대화 → 재접속 후 기록 복원 → 후속 알림 신청·취소·상담방 이동
